# 智慧农业管理系统 — 核心代码说明

## 一、项目架构总览

```
khk/
├── backend/                    # 后端（Python FastAPI）
│   ├── 运行.py                 # 启动入口
│   ├── 种子数据.py              # 演示数据
│   └── app/
│       ├── 主要.py              # FastAPI 应用工厂
│       ├── 数据库.py            # SQLite 连接
│       ├── 模型.py              # 7 张数据库表 ORM
│       ├── 模式.py              # Pydantic 请求/响应模型
│       ├── 安全.py              # JWT + 密码哈希
│       ├── 工具.py              # 中俄双语翻译
│       └── routers/            # API 路由层
│           ├── 认证.py          # 登录/注册
│           ├── 地块.py          # 土地管理 + 轮作
│           ├── 任务.py          # 任务调度
│           ├── 资源.py          # 物资管理 + 消耗记录
│           └── 报告.py          # 数据分析 + 效益核算
└── frontend/                   # 前端（Vue 3 + Vite）
    └── src/
        ├── views/              # 6 个页面组件
        ├── stores/             # Pinia 状态管理
        ├── locales/            # 中俄双语 JSON
        └── router.js           # 路由 + 登录守卫
```

## 二、技术选型

| 层面 | 技术 | 选型理由 |
|------|------|----------|
| 后端框架 | FastAPI | 高性能异步框架，自动生成 Swagger 文档 |
| ORM | SQLAlchemy | Python 最成熟的 ORM |
| 数据库 | SQLite | 零配置文件数据库，适合中小规模农场 |
| 认证 | JWT (HS256) | 无状态令牌，适合前后端分离 |
| 密码加密 | PBKDF2-SHA256 | 10 万次迭代，安全性足够 |
| 前端框架 | Vue 3 (Composition API) | 响应式 UI，组件化开发 |
| 状态管理 | Pinia | Vue 3 官方推荐 |
| 国际化 | vue-i18n v10 | Composition API 模式 |
| 地图 | Leaflet + OpenStreetMap | 开源免费，无需 API Key |
| 打包 | Vite | 快速构建，支持 ESM |

**后端依赖（requirements.txt）：**
```
fastapi>=0.100.0
uvicorn>=0.23.0
sqlalchemy>=2.0.0
python-jose[cryptography]>=3.3.0
pydantic>=2.0.0
```

**前端依赖（package.json）：**
```
vue: 3.4+, vue-router: 4.3+, pinia: 2.1+
vue-i18n: 10.0+, axios: 1.7+, leaflet: 1.9+
vite: 5.4+
```

## 三、数据库设计（7 张表）

### 3.1 数据库连接 — `数据库.py`

```python
from pathlib import Path
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, DeclarativeBase

# 绝对路径确保任意目录启动都能找到数据库
DB_PATH = Path(__file__).parent.parent / "smartagro.db"
SQLALCHEMY_DATABASE_URL = f"sqlite:///{DB_PATH}"

engine = create_engine(
    SQLALCHEMY_DATABASE_URL, connect_args={"check_same_thread": False}
)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

class Base(DeclarativeBase):
    pass

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

### 3.2 用户表 (users)

```python
class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    username = Column(String(50), unique=True, index=True, nullable=False)
    email = Column(String(100), unique=True, nullable=False)
    hashed_password = Column(String(200), nullable=False)
    full_name = Column(String(100))
    phone = Column(String(30))
    role = Column(String(20), default="field_staff")
    # 角色: admin, dispatcher, field_staff, storekeeper
    created_at = Column(DateTime, default=datetime.utcnow)
```

### 3.3 地块表 (land_plots) — 核心表

```python
class LandPlot(Base):
    __tablename__ = "land_plots"

    id = Column(Integer, primary_key=True, index=True)
    cadastral_number = Column(String(50), unique=True, nullable=False)
    name = Column(String(100), nullable=False)          # 中文名
    name_ru = Column(String(100))                       # 俄文名
    area_ha = Column(Float, nullable=False)             # 面积(公顷)
    soil_type = Column(String(100))                     # 土壤类型
    soil_type_ru = Column(String(100))                  # 土壤类型(俄)
    geometry = Column(Text)                             # GeoJSON 几何数据
    hash_sum = Column(String(64))                       # SHA256 校验
    status = Column(String(20), default="active")       # active/fallow/archived
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

### 3.4 轮作表 (crop_rotation)

```python
class CropRotation(Base):
    __tablename__ = "crop_rotation"

    id = Column(Integer, primary_key=True, index=True)
    land_plot_id = Column(Integer, ForeignKey("land_plots.id"), nullable=False)
    crop_name = Column(String(100), nullable=False)
    crop_name_ru = Column(String(100))
    sowing_date = Column(DateTime, nullable=False)      # 播种日期
    harvest_date = Column(DateTime)                     # 收获日期
    yield_amount = Column(Float)                        # 产量(公斤/亩)
    notes = Column(Text)
    notes_ru = Column(Text)

    land_plot = relationship("LandPlot", back_populates="crop_rotations")
```

### 3.5 任务表 (tasks)

```python
class Task(Base):
    __tablename__ = "tasks"

    id = Column(Integer, primary_key=True, index=True)
    title = Column(String(200), nullable=False)
    title_ru = Column(String(200))
    description = Column(Text)
    description_ru = Column(Text)
    task_type = Column(String(30), default="other")
    # 类型: plowing, sowing, harvesting, fertilizing, irrigation, other
    land_plot_id = Column(Integer, ForeignKey("land_plots.id"), nullable=False)
    assigned_to = Column(Integer, ForeignKey("users.id"))   # 执行工人
    created_by = Column(Integer, ForeignKey("users.id"))    # 调度员
    planned_start = Column(DateTime, nullable=False)
    planned_end = Column(DateTime, nullable=False)
    status = Column(String(20), default="pending")
    # 状态: pending, in_progress, completed, cancelled
    priority = Column(Integer, default=1)                   # 1-5 优先级
```

### 3.6 资源表 (resources)

```python
class Resource(Base):
    __tablename__ = "resources"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(100), nullable=False)
    name_ru = Column(String(100))
    resource_type = Column(String(30), default="other")
    # 类型: seeds, fertilizer, fuel, pesticide, other
    unit = Column(String(20), nullable=False)              # 单位: 公斤/升
    unit_ru = Column(String(20))
    quantity = Column(Float, default=0.0)                  # 当前库存
    min_threshold = Column(Float, default=0.0)             # 最低预警阈值
    price_per_unit = Column(Float, default=0.0)            # 单价
    supplier = Column(String(100))
    supplier_ru = Column(String(100))
```

### 3.7 物资消耗表 (material_consumption)

```python
class MaterialConsumption(Base):
    __tablename__ = "material_consumption"

    id = Column(Integer, primary_key=True, index=True)
    task_id = Column(Integer, ForeignKey("tasks.id"), nullable=False)
    resource_id = Column(Integer, ForeignKey("resources.id"), nullable=False)
    land_plot_id = Column(Integer, ForeignKey("land_plots.id"), nullable=False)
    quantity_used = Column(Float, nullable=False)          # 消耗数量
    used_at = Column(DateTime, default=datetime.utcnow)
    notes = Column(Text)
```

### 3.8 工作报告表 (work_reports)

```python
class WorkReport(Base):
    __tablename__ = "work_reports"

    id = Column(Integer, primary_key=True, index=True)
    task_id = Column(Integer, ForeignKey("tasks.id"), nullable=False)
    worker_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    progress_percent = Column(Float, default=0.0)          # 进度百分比
    report_text = Column(Text)
    report_text_ru = Column(Text)
    geo_latitude = Column(Float)                           # GPS 纬度
    geo_longitude = Column(Float)                          # GPS 经度
    reported_at = Column(DateTime, default=datetime.utcnow)
    offline_synced = Column(Integer, default=0)            # 离线同步标记
```

## 四、中俄双语实现原理

### 4.1 数据库层：双字段模式

每个需要翻译的字段都有一对列：主字段 + `_ru` 后缀字段。

```
┌─────────────┬──────────────────────┐
│ name (中文)  │ name_ru (俄文)        │
│ 伊犁麦田     │ Илийское поле        │
├─────────────┼──────────────────────┤
│ soil_type   │ soil_type_ru         │
│ 黑土         │ Чернозём             │
└─────────────┴──────────────────────┘
```

### 4.2 后端翻译工具 — `工具.py`

```python
def tr(obj, lang: str, field_map: dict):
    """若 lang=ru 且有 _ru 字段值，则替换主字段值"""
    if lang != "ru":
        return
    for zh_field, ru_field in field_map.items():
        ru_val = getattr(obj, ru_field, None)
        if ru_val:
            setattr(obj, zh_field, ru_val)

def tr_dict(d: dict, lang: str, field_map: dict):
    """字典版本"""
    if lang != "ru":
        return
    for zh_key, ru_key in field_map.items():
        if d.get(ru_key):
            d[zh_key] = d[ru_key]
```

### 4.3 路由中使用翻译

```python
# 地块 API 中定义字段映射
LAND_MAP = {"name": "name_ru", "soil_type": "soil_type_ru"}
ROT_MAP = {"crop_name": "crop_name_ru", "notes": "notes_ru"}
TASK_MAP = {"title": "title_ru", "description": "description_ru"}
RES_MAP = {"name": "name_ru", "unit": "unit_ru", "supplier": "supplier_ru"}

@router.get("/", response_model=list[LandPlotOut])
def list_lands(lang: str = Query("zh"), db: Session = Depends(get_db)):
    plots = db.query(LandPlot).all()
    for p in plots:
        tr(p, lang, LAND_MAP)      # 俄语模式下自动替换字段值
    return plots
```

### 4.4 前端 Axios 拦截器自动附带语言参数

```javascript
// stores/data.js
const api = axios.create({ baseURL: '/api' })

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  const lang = localStorage.getItem('lang') || 'zh'
  config.params = { ...config.params, lang }  // 每个请求自动附带 ?lang=zh 或 ?lang=ru
  return config
})
```

### 4.5 前端国际化配置 — `i18n.js`

```javascript
import { createI18n } from 'vue-i18n'
import zh from './locales/zh.json'
import ru from './locales/ru.json'

const savedLang = localStorage.getItem('lang') || 'zh'

const i18n = createI18n({
  legacy: false,
  locale: savedLang,
  fallbackLocale: 'zh',
  messages: { ru, zh },
})

export default i18n
```

### 4.6 语言切换按钮 — `Layout.vue`

```javascript
async function toggleLang() {
  const newLang = i18n.global.locale.value === 'ru' ? 'zh' : 'ru'
  i18n.global.locale.value = newLang
  locale.value = newLang
  localStorage.setItem('lang', newLang)
  // 切换后立即刷新所有数据
  await Promise.all([
    data.fetchLands(),
    data.fetchTasks(),
    data.fetchResources(),
    data.fetchAnalytics(),
    data.fetchProfitability(),
  ])
}
```

### 4.7 前端翻译文本示例（zh.json / ru.json）

```json
// zh.json（中文）
{
  "app": { "title": "智慧农业管理系统", "subtitle": "Smart Agriculture" },
  "nav": { "dashboard": "仪表盘", "lands": "土地管理", "tasks": "任务调度" },
  "lands": { "title": "土地管理", "add": "添加地块", "name": "地块名称" }
}
```

```json
// ru.json（俄语）
{
  "app": { "title": "Информационная система", "subtitle": "Умное сельское хозяйство" },
  "nav": { "dashboard": "Панель", "lands": "Участки", "tasks": "Задачи" },
  "lands": { "title": "Управление участками", "add": "Добавить участок", "name": "Название" }
}
```

## 五、核心 API 接口实现

### 5.1 登录接口 — `认证.py`

```python
router = APIRouter(prefix="/api/auth", tags=["auth"])

@router.post("/login", response_model=Token)
def login(data: LoginRequest, db: Session = Depends(get_db)):
    user = db.query(User).filter(User.username == data.username).first()
    if not user or not verify_password(data.password, user.hashed_password):
        raise HTTPException(status_code=401, detail="Invalid credentials")
    token = create_access_token({"sub": str(user.id), "role": user.role})
    return Token(
        access_token=token,
        token_type="bearer",
        user=UserOut.model_validate(user)
    )

@router.post("/register", response_model=UserOut)
def register(data: UserCreate, db: Session = Depends(get_db)):
    existing = db.query(User).filter(
        (User.username == data.username) | (User.email == data.email)
    ).first()
    if existing:
        raise HTTPException(status_code=400, detail="Username or email already exists")
    user = User(
        username=data.username,
        email=data.email,
        hashed_password=hash_password(data.password),
        full_name=data.full_name,
        role=data.role,
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return user
```

### 5.2 地块 CRUD — `地块.py`

```python
router = APIRouter(prefix="/api/lands", tags=["lands"])

# 查询所有地块（支持语言切换）
@router.get("/", response_model=list[LandPlotOut])
def list_lands(lang: str = Query("zh"), db: Session = Depends(get_db)):
    plots = db.query(LandPlot).all()
    for p in plots:
        tr(p, lang, LAND_MAP)
    return plots

# 创建地块（含 GeoJSON 几何校验）
@router.post("/", response_model=LandPlotOut)
def create_land(data: LandPlotCreate, db: Session = Depends(get_db)):
    existing = db.query(LandPlot).filter(
        LandPlot.cadastral_number == data.cadastral_number
    ).first()
    if existing:
        raise HTTPException(status_code=400, detail="Cadastral number already exists")
    plot = LandPlot(
        cadastral_number=data.cadastral_number,
        name=data.name, name_ru=data.name_ru,
        area_ha=data.area_ha,
        soil_type=data.soil_type, soil_type_ru=data.soil_type_ru,
        geometry=data.geometry,
        hash_sum=hashlib.sha256(data.geometry.encode()).hexdigest() if data.geometry else None,
    )
    db.add(plot)
    db.commit()
    db.refresh(plot)
    return plot

# 更新地块
@router.put("/{plot_id}", response_model=LandPlotOut)
def update_land(plot_id: int, data: LandPlotUpdate, db: Session = Depends(get_db)):
    plot = db.query(LandPlot).filter(LandPlot.id == plot_id).first()
    if not plot:
        raise HTTPException(status_code=404, detail="Land plot not found")
    update_data = data.model_dump(exclude_unset=True)
    if update_data.get("geometry"):
        update_data["hash_sum"] = hashlib.sha256(update_data["geometry"].encode()).hexdigest()
    for key, val in update_data.items():
        setattr(plot, key, val)
    db.commit()
    db.refresh(plot)
    return plot

# 删除地块
@router.delete("/{plot_id}")
def delete_land(plot_id: int, db: Session = Depends(get_db)):
    plot = db.query(LandPlot).filter(LandPlot.id == plot_id).first()
    if not plot:
        raise HTTPException(status_code=404, detail="Land plot not found")
    db.delete(plot)
    db.commit()
    return {"message": "Deleted"}
```

### 5.3 物资消耗（自动扣减库存） — `资源.py`

```python
@router.post("/consumption", response_model=MaterialConsumptionOut)
def add_consumption(data: MaterialConsumptionCreate, db: Session = Depends(get_db)):
    resource = db.query(Resource).filter(Resource.id == data.resource_id).first()
    if not resource:
        raise HTTPException(status_code=404, detail="Resource not found")
    if resource.quantity < data.quantity_used:
        raise HTTPException(status_code=400, detail="Insufficient resource quantity")

    resource.quantity -= data.quantity_used   # ⬅ 自动扣减库存
    cons = MaterialConsumption(**data.model_dump())
    db.add(cons)
    db.commit()
    db.refresh(cons)
    return cons
```

### 5.4 数据分析接口 — `报告.py`

```python
@router.get("/analytics", response_model=AnalyticsSummary)
def get_analytics(db: Session = Depends(get_db)):
    total_area = db.query(func.sum(LandPlot.area_ha)).scalar() or 0
    total_plots = db.query(func.count(LandPlot.id)).scalar() or 0
    active_tasks = db.query(func.count(Task.id)).filter(
        Task.status.in_(["pending", "in_progress"])
    ).scalar() or 0
    completed_tasks = db.query(func.count(Task.id)).filter(
        Task.status == "completed"
    ).scalar() or 0
    total_value = db.query(
        func.sum(Resource.quantity * Resource.price_per_unit)
    ).scalar() or 0
    low_stock = db.query(func.count(Resource.id)).filter(
        Resource.quantity <= Resource.min_threshold
    ).scalar() or 0
    return AnalyticsSummary(
        total_land_area=round(total_area, 2),
        total_plots=total_plots,
        active_tasks=active_tasks,
        completed_tasks=completed_tasks,
        total_resources_value=round(total_value, 2),
        low_stock_resources=low_stock,
    )

@router.get("/profitability")
def profitability_by_field(lang: str = Query("zh"), db: Session = Depends(get_db)):
    """各地块效益核算：统计每个地块的物资消耗成本"""
    results = []
    plots = db.query(LandPlot).all()
    for plot in plots:
        consumptions = db.query(MaterialConsumption).filter(
            MaterialConsumption.land_plot_id == plot.id
        ).all()
        total_cost = 0.0
        for c in consumptions:
            resource = db.query(Resource).filter(Resource.id == c.resource_id).first()
            if resource:
                total_cost += c.quantity_used * resource.price_per_unit
        name = plot.name_ru if lang == "ru" and plot.name_ru else plot.name
        results.append({
            "plot_id": plot.id, "plot_name": name,
            "area_ha": plot.area_ha,
            "total_material_cost": round(total_cost, 2),
            "cost_per_ha": round(total_cost / plot.area_ha, 2) if plot.area_ha > 0 else 0,
        })
    return results
```

## 六、安全认证实现

### 6.1 密码哈希 — `安全.py`

```python
import hashlib, os

def hash_password(password: str) -> str:
    """PBKDF2-SHA256，10 万次迭代"""
    salt = os.urandom(32).hex()
    h = hashlib.pbkdf2_hmac("sha256", password.encode(), salt.encode(), 100000)
    return f"{salt}${h.hex()}"

def verify_password(plain: str, hashed: str) -> bool:
    try:
        salt, stored = hashed.split("$", 1)
        h = hashlib.pbkdf2_hmac("sha256", plain.encode(), salt.encode(), 100000)
        return h.hex() == stored
    except (ValueError, AttributeError):
        return False
```

### 6.2 JWT 令牌生成与验证

```python
from jose import jwt, JWTError

SECRET_KEY = "smartagro-secret-key-change-in-production"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 480

def create_access_token(data: dict) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

def decode_token(token: str) -> Optional[dict]:
    try:
        return jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    except JWTError:
        return None
```

## 七、FastAPI 应用入口 — `主要.py`

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse

from .数据库 import engine, Base
from .routers import 认证, 地块, 任务, 资源, 报告

# 自动建表
Base.metadata.create_all(bind=engine)

app = FastAPI(title="智慧农业 API", version="1.0.0")

# CORS 跨域
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 注册 API 路由
app.include_router(认证.router)
app.include_router(地块.router)
app.include_router(任务.router)
app.include_router(资源.router)
app.include_router(报告.router)

# 托管前端静态文件（SPA 模式）
frontend_dist = os.path.join(os.path.dirname(__file__), "../../frontend/dist")
if os.path.isdir(frontend_dist):
    app.mount("/assets", StaticFiles(directory=os.path.join(frontend_dist, "assets")))
    @app.get("/{path:path}")
    async def serve_spa(path: str):
        file_path = os.path.join(frontend_dist, path)
        if os.path.isfile(file_path):
            return FileResponse(file_path)
        return FileResponse(os.path.join(frontend_dist, "index.html"))
```

## 八、启动入口 — `运行.py`

```python
import sys, io, os, webbrowser, threading, uvicorn

# Windows UTF-8 编码修复
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding='utf-8')

if __name__ == "__main__":
    os.chdir(os.path.dirname(os.path.abspath(__file__)))
    print("  智慧农业管理系统  http://localhost:8008")
    threading.Timer(1.5, lambda: webbrowser.open("http://localhost:8008")).start()
    uvicorn.run("app.主要:app", host="0.0.0.0", port=8008, reload=False)
```

## 九、前端核心代码

### 9.1 Vue Router 路由配置 — `router.js`

```javascript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  { path: '/login', name: 'Login', component: () => import('./views/Login.vue') },
  { path: '/', component: () => import('./views/Layout.vue'), meta: { requiresAuth: true },
    children: [
      { path: '', name: 'Dashboard', component: () => import('./views/Dashboard.vue') },
      { path: 'lands', name: 'Lands', component: () => import('./views/Lands.vue') },
      { path: 'tasks', name: 'Tasks', component: () => import('./views/Tasks.vue') },
      { path: 'resources', name: 'Res', component: () => import('./views/Resources.vue') },
      { path: 'reports', name: 'Reports', component: () => import('./views/Reports.vue') },
    ]
  }
]

const router = createRouter({ history: createWebHistory(), routes })

// 登录守卫：未登录自动跳转
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (to.meta.requiresAuth && !token) {
    next('/login')
  } else {
    next()
  }
})

export default router
```

### 9.2 Pinia 状态管理 — `stores/auth.js`

```javascript
import { defineStore } from 'pinia'
import axios from 'axios'

const api = axios.create({ baseURL: '/api' })

export const useAuthStore = defineStore('auth', {
  state: () => ({ user: null, token: localStorage.getItem('token') || null }),
  actions: {
    async login(username, password) {
      const { data } = await api.post('/auth/login', { username, password })
      this.token = data.access_token
      this.user = data.user
      localStorage.setItem('token', data.access_token)
    },
    async register(form) {
      await api.post('/auth/register', form)
    },
    logout() {
      this.token = null
      this.user = null
      localStorage.removeItem('token')
    },
  },
})
```

### 9.3 Leaflet 地图集成 — `Lands.vue`（核心片段）

```javascript
async function initMap() {
  const L = await import('leaflet')         // 动态导入，减小首屏体积
  if (mapInstance) mapInstance.remove()

  // 创建地图，中心为新疆伊犁，隐藏右下角水印
  mapInstance = L.map(mapContainer.value, { attributionControl: false })
    .setView([43.92, 81.29], 12)

  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png')
    .addTo(mapInstance)

  // 渲染所有地块为绿色多边形
  store.lands.forEach((plot) => {
    if (plot.geometry) {
      const geo = JSON.parse(plot.geometry)
      if (geo.type === 'Polygon') {
        const coords = geo.coordinates[0].map(([lng, lat]) => [lat, lng])
        const layer = L.polygon(coords, {
          color: '#22c55e', fillColor: '#22c55e',
          fillOpacity: 0.4, weight: 2
        }).bindPopup(`<b>${plot.name}</b><br>${plot.area_ha} ha`)
          .addTo(mapInstance)
        plotLayers[plot.id] = layer  // 保存引用，用于高亮定位
      } else if (geo.type === 'Point') {
        const [lng, lat] = geo.coordinates
        L.marker([lat, lng])
          .bindPopup(`<b>${plot.name}</b><br>${plot.area_ha} ha`)
          .addTo(mapInstance)
      }
    }
  })
}

// 点击表格行定位到地块
async function locatePlot(plot) {
  if (!showMap.value) { showMap.value = true; await nextTick(); initMap() }
  await nextTick()
  // 高亮选中地块（黄色），自动飞行动画 + 弹出信息窗
  const layer = plotLayers[plot.id]
  layer.setStyle({ color: '#eab308', fillColor: '#eab308', fillOpacity: 0.6, weight: 4 })
  const geo = JSON.parse(plot.geometry)
  const coords = geo.coordinates[0].map(([lng, lat]) => [lat, lng])
  mapInstance.fitBounds(coords, { padding: [50, 50], maxZoom: 15 })
  setTimeout(() => layer.openPopup(), 600)
}
```

### 9.4 GeoJSON 几何数据自动生成

```javascript
// 用户只需输入经纬度，点击按钮自动生成 GeoJSON
function genPoint() {
  const lat = parseFloat(geoLat.value), lng = parseFloat(geoLng.value)
  form.value.geometry = JSON.stringify({ type: 'Point', coordinates: [lng, lat] })
}

function genSquare() {
  const lat = parseFloat(geoLat.value), lng = parseFloat(geoLng.value)
  const d = 0.02  // 约 2 公里边长的矩形
  form.value.geometry = JSON.stringify({
    type: 'Polygon',
    coordinates: [[
      [lng - d, lat - d], [lng + d, lat - d],
      [lng + d, lat + d], [lng - d, lat + d],
      [lng - d, lat - d],
    ]],
  })
}
```

## 十、部署方式

### 10.1 开发环境
```bash
# PyCharm 中右键运行 运行.py，自动打开 http://localhost:8008
```

### 10.2 生产构建
```bash
cd frontend && npm run build    # 构建前端到 dist/
cd ../backend && python 运行.py  # FastAPI 直接托管前端静态文件
```

### 10.3 Docker 部署
```yaml
# docker-compose.yml
services:
  backend:
    build: ./backend
    ports: ["8008:8008"]
    volumes: ["./backend/data:/app/data"]
```

## 十一、演示数据

运行 `种子数据.py` 自动填充：
- 4 个用户（admin/admin123、dispatcher/disp123、worker1/work123、storekeeper/store123）
- 3 个新疆伊犁地块（麦田 120.5ha、玉米地 85.3ha、大豆田 200ha）
- 3 条轮作记录（冬小麦→夏玉米→春玉米）
- 4 个任务（深耕 in_progress、播种 pending、收割 completed、施肥 pending）
- 5 种资源（种子、复合肥、柴油、除草剂、玉米种子）
- 2 条工作报告
